---
phase: 04.1-navbar-navigation-between-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/BottomTabBar.tsx
  - src/components/layout/MobileTopBar.tsx
  - src/components/layout/NavItem.tsx
autonomous: true
requirements: [NAV-SHELL]

must_haves:
  truths:
    - "AppLayout renders a persistent left sidebar on desktop (md: and above)"
    - "AppLayout renders a fixed bottom tab bar on mobile (below md:)"
    - "AppLayout renders a slim top bar on mobile showing page title and user avatar"
    - "AppLayout renders the existing desktop top header (title + SyncIndicator) on desktop only"
    - "Nav items are filtered to roles the user has access to (hidden, not grayed)"
    - "Active nav item is visually highlighted using current pathname"
    - "User identity (avatar or initials + name) appears in sidebar footer on desktop and top bar on mobile"
    - "No-role users see 'Contact your admin to get access' message"
  artifacts:
    - path: "src/components/layout/AppLayout.tsx"
      provides: "Outer shell with Outlet, sidebar, bottom tabs, headers"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Desktop always-expanded sidebar with nav items and user identity"
    - path: "src/components/layout/BottomTabBar.tsx"
      provides: "Mobile fixed bottom tab bar with icon + label per tab"
    - path: "src/components/layout/MobileTopBar.tsx"
      provides: "Mobile slim top bar with page title and user avatar"
    - path: "src/components/layout/NavItem.tsx"
      provides: "Shared nav item with icon + label + active state"
  key_links:
    - from: "src/components/layout/AppLayout.tsx"
      to: "react-router Outlet"
      via: "import { Outlet } from 'react-router'"
      pattern: "Outlet"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/context/AuthContext.tsx"
      via: "useRole() for nav item filtering"
      pattern: "useRole"
    - from: "src/components/layout/MobileTopBar.tsx"
      to: "@clerk/clerk-react"
      via: "useUser() guarded by isDevModeWithoutCredentials"
      pattern: "useUser"
---

<objective>
Create all layout shell components for the responsive navbar: AppLayout (Outlet wrapper), Sidebar (desktop), BottomTabBar (mobile), MobileTopBar (mobile), and NavItem (shared).

Purpose: Provide a persistent nav shell that gives users role-aware navigation between sections. Currently users are hard-redirected to a single section with no manual navigation.

Output: Five new files under src/components/layout/ that compose into a responsive layout shell.
</objective>

<execution_context>
@/Users/dennyleonardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dennyleonardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.1-navbar-navigation-between-sections/04.1-CONTEXT.md
@.planning/phases/04.1-navbar-navigation-between-sections/04.1-RESEARCH.md
@src/routes/index.tsx
@src/routes/protected.tsx
@src/context/AuthContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NavItem and nav data hook</name>
  <files>src/components/layout/NavItem.tsx</files>
  <action>
Create `src/components/layout/NavItem.tsx` with:

1. A `NavItem` component accepting `{ path: string; label: string; icon: LucideIcon }` props.
   - Uses `useLocation()` from `react-router` to detect active state: `pathname === path || pathname.startsWith(path + '/')`
   - Active: `bg-primary text-primary-foreground`
   - Inactive: `text-muted-foreground hover:bg-accent hover:text-accent-foreground`
   - Renders as `<Link to={path}>` with icon (w-5 h-5 shrink-0) + label text side by side
   - Tailwind classes: `flex items-center gap-3 px-3 py-2 rounded-md text-sm font-medium transition-colors`
   - Use `cn()` from `@/lib/utils` for conditional classes

2. Export a `useNavItems()` hook in the same file:
   - Imports `useRole` and `isDevModeWithoutCredentials` from `@/context/AuthContext`
   - Defines nav config array:
     ```ts
     const NAV_CONFIG = [
       { label: "Builder", path: "/admin/builder", icon: LayoutGrid, requiresAdmin: true },
       { label: "Fill Forms", path: "/worker/forms", icon: ClipboardList, requiresWorker: true },
       { label: "Reviews", path: "/reviewer/dashboard", icon: CheckSquare, requiresReviewer: true },
     ];
     ```
   - In dev mode (`isDevModeWithoutCredentials`), returns all three items (mock user has all roles)
   - In real mode, filters: if `item.requiresAdmin && !isAdmin` → exclude; `item.requiresWorker && !isWorker` → exclude; `item.requiresReviewer && !isReviewer` → exclude
   - Icons: `LayoutGrid`, `ClipboardList`, `CheckSquare` from `lucide-react`
   - Returns the filtered array

3. Export a `useUserIdentity()` hook in the same file:
   - If `isDevModeWithoutCredentials` → return `{ name: "Dev User", imageUrl: null }`
   - Otherwise call `useUser()` from `@clerk/clerk-react` and return:
     ```ts
     {
       name: [user?.firstName, user?.lastName].filter(Boolean).join(" ") || "User",
       imageUrl: user?.imageUrl ?? null,
     }
     ```
   - This guards against `useUser()` crash outside ClerkProvider

4. Export a `UserAvatar` component:
   - Props: `{ imageUrl: string | null; name: string; size?: "sm" | "md" }` (default `"md"`)
   - sm: `w-7 h-7`, md: `w-8 h-8`
   - If `imageUrl`: `<img src={imageUrl} alt="Avatar" className="... rounded-full object-cover" />`
   - Else: `<div className="... rounded-full bg-muted flex items-center justify-center text-xs font-medium">` showing initials (first char of each word in name, max 2)

No default export. Named exports: `NavItem`, `useNavItems`, `useUserIdentity`, `UserAvatar`.
  </action>
  <verify>
    <automated>cd /Users/dennyleonardo/Code/nexus && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>File exists at src/components/layout/NavItem.tsx with all four exports</manual>
  </verify>
  <done>NavItem.tsx compiles without TypeScript errors; exports NavItem, useNavItems, useUserIdentity, UserAvatar</done>
</task>

<task type="auto">
  <name>Task 2: Create Sidebar, BottomTabBar, MobileTopBar, and AppLayout</name>
  <files>
    src/components/layout/Sidebar.tsx
    src/components/layout/BottomTabBar.tsx
    src/components/layout/MobileTopBar.tsx
    src/components/layout/AppLayout.tsx
  </files>
  <action>
Create the four remaining layout components. All import from `NavItem.tsx`.

**src/components/layout/Sidebar.tsx**
- Desktop only: `hidden md:flex flex-col w-64 border-r bg-background shrink-0`
- Top: App logo/title area: `<div className="px-4 py-4 border-b"><h1 className="text-lg font-bold">Nexus QC Forms</h1></div>`
- Middle (`flex-1 overflow-y-auto py-4 px-2`): Maps `useNavItems()` to `<NavItem />` for each item, full width with `w-full`
- No-role state: if `navItems.length === 0` (and not dev mode), show `<p className="px-3 text-sm text-muted-foreground">Contact your admin to get access</p>`
- Bottom (mt-auto border-t py-4 px-3): `UserAvatar` + name from `useUserIdentity()` side by side (`flex items-center gap-2 text-sm font-medium`)
- Import `useNavItems`, `useUserIdentity`, `UserAvatar`, `NavItem` from `./NavItem`

**src/components/layout/BottomTabBar.tsx**
- Mobile only: `md:hidden fixed bottom-0 left-0 right-0 border-t bg-background z-40`
- Inner: `flex items-center justify-around h-14`
- Maps `useNavItems()` to tab buttons — each is a `<Link>` with icon centered above label, column flex:
  - `flex flex-col items-center gap-0.5 px-3 py-1.5 text-xs font-medium`
  - Active: `text-primary`, Inactive: `text-muted-foreground`
  - Icon: `w-5 h-5`, Label: `text-xs`
  - Use `useLocation()` to detect active, same logic as NavItem
- If no nav items: show nothing (return null — no-role users see sidebar message, not bottom tab)
- Import `useNavItems` from `./NavItem`; `Link`, `useLocation` from `react-router`

**src/components/layout/MobileTopBar.tsx**
- Mobile only: `md:hidden border-b bg-background px-4 h-12 flex items-center justify-between shrink-0`
- Left: Current section name derived from `useLocation().pathname`:
  - `/admin/builder` → "Builder"
  - `/worker/forms` → "Fill Forms"
  - `/reviewer/dashboard` → "Reviews"
  - `/` or unknown → "Nexus QC Forms"
  - `<span className="font-semibold text-sm">{sectionName}</span>`
- Right: `UserAvatar` (size="sm") + no name text (keeps top bar slim)
- Import `useUserIdentity`, `UserAvatar` from `./NavItem`; `useLocation` from `react-router`

**src/components/layout/AppLayout.tsx**
- Primary export: `export default function AppLayout()`
- Structure:
  ```tsx
  <div className="flex min-h-screen bg-background">
    <Sidebar />
    <div className="flex flex-col flex-1 min-w-0">
      <MobileTopBar />
      {/* Desktop top header — hidden on mobile */}
      <header className="hidden md:flex border-b bg-background px-6 py-3 items-center justify-between shrink-0">
        <h1 className="text-xl font-bold">Nexus QC Forms</h1>
        <SyncIndicator />
      </header>
      <main className="flex-1 overflow-auto pb-16 md:pb-0">
        <Outlet />
      </main>
      <BottomTabBar />
    </div>
  </div>
  ```
- Import `SyncIndicator` from `@/components/sync/SyncIndicator`
- Import `Outlet` from `react-router`
- Wrap `SyncIndicator` in try/catch or error boundary — it may throw in dev mode. Use:
  ```tsx
  function SafeSyncIndicator() {
    try {
      return <SyncIndicator />;
    } catch {
      return null;
    }
  }
  ```
  Use `SafeSyncIndicator` in the desktop header instead of `SyncIndicator` directly.
  </action>
  <verify>
    <automated>cd /Users/dennyleonardo/Code/nexus && npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>All four files exist under src/components/layout/</manual>
  </verify>
  <done>All layout components compile without TypeScript errors; AppLayout uses Outlet from react-router</done>
</task>

</tasks>

<verification>
After both tasks:
- Run `npx tsc --noEmit` — zero errors expected
- Confirm 5 files exist: NavItem.tsx, Sidebar.tsx, BottomTabBar.tsx, MobileTopBar.tsx, AppLayout.tsx
- Note: AppLayout is not yet wired into routing — that happens in Plan 02
</verification>

<success_criteria>
- All 5 layout components created and TypeScript-clean
- AppLayout uses `<Outlet />` from react-router (not children prop)
- NavItem uses `useLocation()` for active state (not window.location)
- useUserIdentity() guards `useUser()` behind isDevModeWithoutCredentials check
- No new npm packages installed
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-navbar-navigation-between-sections/04.1-01-SUMMARY.md`
</output>
