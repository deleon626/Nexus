name: nexus_mobile
description: QC Traceability Mobile App for Nexus System
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.0.0
  camera: ^0.10.5
  path_provider: ^2.1.1
  sqflite: ^2.3.0
  provider: ^6.1.1
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
