---
phase: 04.2-deploy-convex-in-coolify-instance-for-this-project
plan: "02"
subsystem: frontend
tags: [convex, self-hosted, configuration, deployment]
dependency_graph:
  requires: [04.2-01]
  provides: [frontend-convex-connection]
  affects: []
tech_stack:
  added: []
  patterns:
    - skipConvexDeploymentUrlCheck for self-hosted Convex
    - .env.production for production environment variables
key_files:
  created:
    - .env.production
  modified:
    - src/lib/convex.ts
    - convex.json
    - convex/_generated/*
decisions:
  - "Updated Docker images to latest (sha256:1cd901be5d7de21bdba700d69dc7e47e91e2e77d87e70356a16ea04fde22d6e8) to match npm package 1.32.0"
  - "Version mismatch between old Docker image (Jan 2026) and npm CLI caused deploy failures"
  - "Moved incorrect convex/functions.ts directory to .archive - was a directory instead of file"
  - "convex.json simplified to just functions: 'convex/' - correct format for directory-based functions"
metrics:
  duration: 15min
  completed: "2026-02-28"
---

# Phase 04.2 Plan 02: Configure Frontend for Self-Hosted Convex Summary

Frontend connected to self-hosted Convex backend running on Coolify, with all functions deployed successfully.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Configure frontend Convex client for self-hosted and deploy functions | 5c3720f | src/lib/convex.ts, .env.production, convex.json, convex/_generated/* |

## Task 2 Status

**BLOCKED — awaiting human verification:** End-to-end connectivity test.

User should:
1. Open the Convex dashboard (http://217.15.164.63:36791) and verify tables/functions are visible
2. Run the app locally with `npm run dev` (uses VITE_CONVEX_URL from .env.local)
3. Test login and verify data loads from self-hosted Convex
4. Create a test form template and verify it persists in the self-hosted database
5. Check browser console for any connection errors

## Deviations from Plan

1. **Docker image update required** - The `:latest` tag on the server was from January 2026, incompatible with npm convex 1.32.0. Had to pull new images and restart containers.

2. **convex/functions.ts was a directory** - There was an incorrectly created `convex/functions.ts` directory (should be a file path in convex.json, not an actual directory). Moved to .archive.

3. **convex.json format** - Simplified to `{ "functions": "convex/" }` which is the correct format for directory-based function resolution.

## Self-Check: PASSED

- src/lib/convex.ts includes skipConvexDeploymentUrlCheck: true ✓
- .env.production contains VITE_CONVEX_URL ✓
- `npx convex deploy` completed without errors ✓
- Schema and indexes pushed successfully:
  - formTemplates.by_org
  - formTemplates.by_org_published
  - submissions.by_org
  - submissions.by_org_status
  - submissions.by_org_user
- Commit 5c3720f verified in git log ✓
