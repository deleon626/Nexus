---
phase: 06-fix-submission-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/submissions.ts
  - src/db/sync/worker.ts
  - src/features/formFilling/pages/FormFillingPage.tsx
  - src/lib/convexHttpClient.ts
autonomous: true
requirements: [REVW-01, REVW-02, REVW-03, REVW-04]

must_haves:
  truths:
    - "Worker submits a form and it appears in reviewer dashboard within seconds"
    - "Worker sees their own submissions with real-time status updates"
    - "Reviewer can view full submission details including form data"
    - "Duplicate submissions (same localId) are rejected by Convex"
    - "Sync worker retries failed submissions automatically"
  artifacts:
    - path: "convex/submissions.ts"
      provides: "createSubmission mutation with idempotency"
      exports: ["createSubmission"]
    - path: "src/db/sync/worker.ts"
      provides: "Actual Convex API calls instead of TODO stub"
    - path: "src/lib/convexHttpClient.ts"
      provides: "Shared ConvexHttpClient instance for non-React code"
    - path: "src/features/formFilling/pages/FormFillingPage.tsx"
      provides: "templateName and workerName in submission payload, immediate sync trigger"
  key_links:
    - from: "src/db/sync/worker.ts"
      to: "convex/submissions.ts"
      via: "ConvexHttpClient calling createSubmission mutation"
      pattern: "convexClient\\.mutation.*createSubmission"
    - from: "src/features/formFilling/pages/FormFillingPage.tsx"
      to: "src/db/sync/worker.ts"
      via: "triggerSync() call after adding to queue"
      pattern: "triggerSync"
---

<objective>
Wire the submission sync pipeline so IndexedDB submissions reach Convex, enabling reviewer dashboards and worker status views to show real data.

Purpose: Close P1 critical path gap — reviewer dashboard and worker status list exist but are empty because the sync worker has a TODO stub instead of actual Convex calls.
Output: Working end-to-end submission pipeline from form submit to reviewer dashboard.
</objective>

<execution_context>
@/Users/dennyleonardo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dennyleonardo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-fix-submission-pipeline/06-RESEARCH.md
@convex/submissions.ts
@convex/schema.ts
@src/db/sync/worker.ts
@src/db/sync/queue.ts
@src/db/types.ts
@src/features/formFilling/pages/FormFillingPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add createSubmission mutation and ConvexHttpClient</name>
  <files>convex/submissions.ts, convex/schema.ts, src/lib/convexHttpClient.ts</files>
  <action>
  1. In `convex/schema.ts`, add a `localId` field to the submissions table:
     - Add `localId: v.string()` to the submissions table definition
     - Add an index `by_localId` on `['localId']` for idempotency lookups

  2. In `convex/submissions.ts`, add a `createSubmission` mutation:
     - Args: localId (string), batchNumber (string), templateId (string), templateName (string), orgId (string), userId (string), workerName (string), data (any), photos (array of string)
     - Handler:
       a. Verify auth via `ctx.auth.getUserIdentity()` — throw if not authenticated
       b. Check idempotency: query `submissions` table with index `by_localId` where localId equals args.localId. If found, return `{ success: true, id: existing._id, duplicate: true }` (skip insert)
       c. Insert into submissions: all args fields + `status: 'pending'`, `createdAt: Date.now()`, `updatedAt: Date.now()`
       d. Return `{ success: true, id: newDoc._id, duplicate: false }`

  3. Create `src/lib/convexHttpClient.ts`:
     - Import `ConvexHttpClient` from 'convex/browser'
     - Read CONVEX_URL from `import.meta.env.VITE_CONVEX_URL`
     - Export a singleton `convexHttpClient` instance
     - This is for non-React code (sync worker) that can't use hooks
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Check convex/submissions.ts has createSubmission export, schema.ts has localId field and by_localId index</manual>
  </verify>
  <done>createSubmission mutation exists with idempotency check, ConvexHttpClient singleton is available for import</done>
</task>

<task type="auto">
  <name>Task 2: Wire sync worker to call Convex and trigger immediate sync</name>
  <files>src/db/sync/worker.ts, src/features/formFilling/pages/FormFillingPage.tsx</files>
  <action>
  1. In `src/db/sync/worker.ts`:
     - Import `convexHttpClient` from `@/lib/convexHttpClient`
     - Import `api` from `@convex/_generated/api`
     - Replace the TODO stub in `processItem()` (lines 30-32: the `new Promise(resolve => setTimeout(resolve, 1000))`) with an actual Convex mutation call:
       ```
       const payload = item.payload;
       const result = await convexHttpClient.mutation(api.submissions.createSubmission, {
         localId: payload.localId,
         batchNumber: payload.batchNumber,
         templateId: payload.templateId,
         templateName: payload.templateName || 'Unknown Form',
         orgId: payload.orgId,
         userId: payload.userId,
         workerName: payload.workerName || 'Unknown Worker',
         data: payload.data,
         photos: payload.photos || [],
       });
       ```
     - Note: The ConvexHttpClient requires auth token. Before the mutation call, get the Clerk token:
       - Import `Clerk` or use `window.Clerk?.session?.getToken({ template: "convex" })` to get the JWT
       - Call `convexHttpClient.setAuth(token)` before the mutation
       - If no token available (user not logged in), throw error to trigger retry

  2. In `src/features/formFilling/pages/FormFillingPage.tsx`:
     - Import `triggerSync` from `@/db/sync`
     - Add `templateName` and `workerName` to the submission object in `handleConfirmSubmit`:
       - `templateName: selectedForm.name`
       - `workerName`: Get from `useUser()` hook — import `useUser` from `@clerk/clerk-react`, extract `user?.fullName || user?.firstName || 'Worker'` at the top of the component
     - After `db.syncQueue.add(...)`, call `triggerSync()` to process immediately instead of waiting 30 seconds
     - Apply the same changes to `handleSubmitAndStartNew` (same submission + sync pattern)
     - Also add `templateName` and `workerName` to the payload in the syncQueue item so the sync worker has access to them

  Note: The `useUser()` hook should be called at the component level (top of FormFillingPage), not inside callbacks. Store the user info and reference it in handlers.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -30</automated>
    <manual>Submit a form, check reviewer dashboard shows the submission. Check worker status list shows the submission with 'pending' status.</manual>
  </verify>
  <done>Sync worker calls createSubmission mutation on Convex. FormFillingPage triggers immediate sync after submit. Submissions flow from IndexedDB to Convex in real-time.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles without errors: `npx tsc --noEmit`
2. Worker submits form -> submission appears in reviewer dashboard (Convex listPendingSubmissions query)
3. Worker sees their submission in status list (Convex listWorkerSubmissions query)
4. Submitting same form twice with same localId does not create duplicate in Convex
5. Reviewer can approve/reject submissions (existing mutations still work)
</verification>

<success_criteria>
- createSubmission mutation exists in convex/submissions.ts with idempotency via localId
- Sync worker calls Convex instead of TODO stub
- FormFillingPage includes templateName and workerName in submission payload
- Immediate sync triggered after form submission
- Reviewer dashboard shows pending submissions after worker sync
- Worker status list shows real-time updates
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-submission-pipeline/06-01-SUMMARY.md`
</output>
